server:
  port: 8082

spring:
  datasource:
    url: jdbc:h2:mem:analysisdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create

provider:
  url: ${PROVIDER_URL:http://localhost:8089}
