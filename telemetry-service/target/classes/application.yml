server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:telemetrydb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create

analysis:
  url: ${ANALYSIS_URL:http://localhost:8082}
